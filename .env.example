# Database Configuration
# IMPORTANT: Set these environment variables before running the application
DB_URL=jdbc:postgresql://localhost:5432/centraldb
DB_USERNAME=your_db_username
DB_PASSWORD=your_secure_db_password

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_if_required

# Instructions:
# 1. Copy this file to .env
# 2. Update the values with your actual database credentials
# 3. Make sure .env is added to .gitignore to prevent accidental commits
# 4. The DB_PASSWORD is required and must be set as an environment variable